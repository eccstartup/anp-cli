# 构建产物
/bin/
/dist/

# Go
*.test
*.out
*.prof
coverage.out

# 编辑器 / OS
.idea/
.vscode/
.DS_Store
.workbuddy/
delivery/

# Claude Code：机器相关的本地配置不提交；
# 共享的 .claude/settings.json、.claude/agents 等可以正常提交
.claude/settings.local.json
.claude/history.jsonl
.claude/todos.json

# ANP 运行时密钥 / 凭据（严禁提交）
*.pem
*.key
*.jwk
*.p12
*.pfx

# 本地 ANP 工作区残留（测试或 ANP_WORKSPACE 指向仓库时产生）
e2ee/
anp.db
anp.db-shm
anp.db-wal
config.yaml
config.yaml.lock
identities/
*.log
*.pid

